// load all the things we need
var rek = require('rekuire')
    , LocalStrategy = require('passport-local').Strategy
    , UserModel = rek('db/mongoose').UserModel;
    
//Exporting passport handler module
module.exports = function(passport) {

};
